---
import Layout from '../../layouts/Layout.astro';
import { getCollection } from 'astro:content';
import { ArrowRight, Server, Cpu, Database, ShieldCheck, MessageSquare, ChevronRight } from 'lucide-astro';

const products = await getCollection('products');

// Group products by category
const categories = ['Server', 'Workstation', 'Storage'];
---

<Layout title="产品中心 | Dell 服务器、工作站、存储现货供应 | 上海皓戴" keywords="Dell服务器报价, PowerEdge R760, Precision工作站, PowerVault存储">
  <!-- Hero Section -->
  <section class="relative py-24 bg-slate-900 overflow-hidden">
    <div class="absolute inset-0 bg-[radial-gradient(circle_at_bottom_left,_var(--tw-gradient-stops))] from-blue-900/40 via-slate-900 to-slate-900"></div>
    <div class="relative z-10 max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 text-center">
      <nav class="flex justify-center items-center gap-2 text-sm text-slate-400 mb-8">
        <a href="/haodell/" class="hover:text-white transition-colors">首页</a>
        <ChevronRight class="w-4 h-4" />
        <span class="text-slate-200">产品中心</span>
      </nav>
      <h1 class="text-5xl md:text-6xl font-bold text-white mb-8 tracking-tight">
        企业级硬件 <span class="text-blue-500">解决方案</span>
      </h1>
      <p class="text-xl text-slate-300 max-w-3xl mx-auto leading-relaxed">
        上海皓戴严选戴尔（Dell）主力机型，现货储备，支持深度定制（CTO），为您的业务提供最坚实的算力支撑。
      </p>
    </div>
  </section>

  <!-- Category Filter (Sticky) -->
  <div class="sticky top-20 z-30 bg-theme-bg/80 backdrop-blur-md border-b border-theme-border">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
      <div class="flex items-center justify-center space-x-8 py-4 overflow-x-auto no-scrollbar">
        <a href="#all" class="text-sm font-bold text-blue-600 border-b-2 border-blue-600 pb-4 -mb-4">全部产品</a>
        {categories.map(cat => (
          <a href={`#${cat.toLowerCase()}`} class="text-sm font-bold text-theme-muted hover:text-theme-text transition-colors pb-4 -mb-4">
            {cat === 'Server' ? '服务器' : cat === 'Workstation' ? '工作站' : '存储设备'}
          </a>
        ))}
      </div>
    </div>
  </div>

  <main class="py-20 bg-theme-bg">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 space-y-32">
      {categories.map(category => {
        const categoryProducts = products.filter(p => p.data.category === category);
        if (categoryProducts.length === 0) return null;

        return (
          <section id={category.toLowerCase()} class="scroll-mt-32">
            <div class="flex items-center gap-4 mb-12">
              <div class="w-12 h-12 rounded-2xl bg-blue-600 flex items-center justify-center text-white shadow-lg shadow-blue-600/20">
                {category === 'Server' && <Server class="w-6 h-6" />}
                {category === 'Workstation' && <Cpu class="w-6 h-6" />}
                {category === 'Storage' && <Database class="w-6 h-6" />}
              </div>
              <div>
                <h2 class="text-3xl font-bold text-theme-text">
                  {category === 'Server' ? 'PowerEdge 服务器' : category === 'Workstation' ? 'Precision 工作站' : 'PowerVault 存储'}
                </h2>
                <p class="text-theme-muted">
                  {category === 'Server' ? '稳定可靠的计算基石' : category === 'Workstation' ? '释放无限可能的生产力工具' : '安全高效的数据管理中心'}
                </p>
              </div>
            </div>

            <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-8">
              {categoryProducts.map(product => (
                <div class="group bg-theme-surface border border-theme-border rounded-3xl overflow-hidden hover:shadow-2xl hover:border-blue-500/50 transition-all duration-500 flex flex-col">
                  <div class="aspect-[4/3] bg-theme-bg relative overflow-hidden p-8 flex items-center justify-center">
                    <img 
                      src={product.data.image} 
                      alt={product.data.title}
                      class="w-full h-full object-contain group-hover:scale-110 transition-transform duration-700"
                    />
                    {product.data.badge && (
                      <div class="absolute top-6 left-6">
                        <span class="px-3 py-1 bg-blue-600 text-white text-[10px] font-bold uppercase tracking-widest rounded-full shadow-lg">
                          {product.data.badge}
                        </span>
                      </div>
                    )}
                  </div>
                  <div class="p-8 flex-1 flex flex-col">
                    <h3 class="text-xl font-bold text-theme-text mb-3 group-hover:text-blue-600 transition-colors">
                      {product.data.title}
                    </h3>
                    <p class="text-theme-muted text-sm line-clamp-2 mb-6 flex-1">
                      {product.data.description}
                    </p>
                    
                    <div class="space-y-3 mb-8">
                      {product.data.specs.slice(0, 3).map(spec => (
                        <div class="flex justify-between items-center text-xs">
                          <span class="text-theme-muted font-medium">{spec.label}</span>
                          <span class="text-theme-text font-bold truncate ml-4 max-w-[160px]">{spec.value}</span>
                        </div>
                      ))}
                    </div>

                    <div class="flex items-center justify-between pt-6 border-t border-theme-border">
                      <span class="text-lg font-bold text-theme-text">{product.data.price}</span>
                      <a href={`/haodell/products/${product.slug}`} class="inline-flex items-center text-sm font-bold text-blue-600 hover:text-blue-500 transition-colors">
                        查看详情 <ArrowRight class="w-4 h-4 ml-1" />
                      </a>
                    </div>
                  </div>
                </div>
              ))}
            </div>
          </section>
        )
      })}
    </div>
  </main>

  <!-- CTA Section -->
  <section class="py-20 bg-slate-900">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 text-center">
      <h2 class="text-3xl md:text-4xl font-bold text-white mb-8">没找到合适的配置？</h2>
      <p class="text-xl text-slate-300 mb-12 max-w-2xl mx-auto">
        我们支持 Dell 全系列产品的按需定制 (CTO)，无论是特殊的内存容量还是特定的显卡需求，我们的工程师都能为您提供专业的配置建议。
      </p>
      <div class="flex flex-col sm:flex-row justify-center gap-4">
        <a href="/haodell/#contact" class="px-8 py-4 bg-blue-600 hover:bg-blue-500 text-white font-bold rounded-xl transition-all shadow-lg shadow-blue-500/30">
          立即咨询销售
        </a>
        <a href="tel:400-888-8888" class="px-8 py-4 bg-white/10 text-white border border-white/20 hover:bg-white/20 font-bold rounded-xl transition-all backdrop-blur-sm flex items-center justify-center">
          <MessageSquare class="w-5 h-5 mr-2" />
          400-888-8888
        </a>
      </div>
    </div>
  </section>
</Layout>
